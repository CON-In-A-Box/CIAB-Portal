<?php

// CON-In-A-Box Site Config (module)

// DBConfig, passwords, etc

// Which Database backend to use - options in backends/
$DB_BACKEND = "mysqlpdo.inc";

// Do not share this info - As we quired by the above backend
$DBHOST = "mysqlhome.convergenceevents.org"; // Where the DB Runs
$DBUSER = "ce_portaldev"; // The Database Username
$DBNAME = "ce_signinportal_dev"; // The Database Name
$DBPASS = "CxHOhY6eAYHI"; // *PLAIN TEXT* database user's password

?>
